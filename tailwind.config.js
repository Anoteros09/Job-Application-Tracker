/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: "#121212", // Dark background
        secondary: "#1E1E1E", // Slightly lighter dark for contrast
        "primary-text": "#FFFFFF", // Main text color (white)
        "secondary-text": "#B0B0B0", // Subdued text color (gray)
      },
    },
  },
  plugins: [],
};
